# Core Framework
# fastapi 0.115+ pulls starlette 0.40+, which fixes
# CVE-2024-47874 and CVE-2025-54121 transitively.
fastapi==0.115.0
uvicorn==0.24.0.post1
# python-multipart bumped from 0.0.6 -> 0.0.27 to fix:
#   PYSEC-2024-38, CVE-2024-53981, CVE-2026-24486,
#   CVE-2026-40347, CVE-2026-42561
python-multipart==0.0.27

# Data Validation
pydantic[email]>=2.11.7,<3.0.0
pydantic-settings>=2.0.0

# HTTP & API
# requests bumped from 2.31.0 -> 2.33.0 to fix:
#   CVE-2024-35195, CVE-2024-47081, CVE-2026-25645
requests==2.33.0
aiohttp>=3.9.0

# Data Processing
numpy==2.0.2

# PDF Processing
PyPDF2==3.0.1

# Environment
# python-dotenv bumped from 1.1.1 -> 1.2.2 to fix CVE-2026-28684
python-dotenv==1.2.2

# AI/ML Services
landingai-ade==1.2.0
openai==2.24.0

# HTML Parsing
beautifulsoup4==4.12.2

# Database & Storage
supabase>=2.24.0
PyJWT>=2.8.0

# Vector DB
qdrant-client>=1.9.0

# Task Queue
arq>=0.25.0

# PDF Report Generation
# xhtml2pdf 0.2.16: CVE-2024-25885 has no fix available upstream as of
# 2026-05. Practical risk is low: xhtml2pdf is a legacy path; primary
# PDF rendering goes through Playwright/Chromium (see Dockerfile.worker
# + backend/render/pdf_chromium.py). xhtml2pdf only used by older
# Report v1 codepath that's been superseded. Tracked as accepted risk.
xhtml2pdf==0.2.16
python-bidi==0.4.2

# PDF Report Generation v2 (Phase 3 — Chromium-based)
# `playwright install chromium` must be run after pip install (see Dockerfile.worker).
# Web container does NOT need Chromium — only the worker renders PDFs.
playwright>=1.40.0
Jinja2>=3.1.0
markdown>=3.5

# Rate Limiting
slowapi>=0.1.9

# FinBot - Market Data
yfinance>=0.2.36
finnhub-python>=2.4.19

# Observability
sentry-sdk[fastapi]>=2.18.0,<3.0.0
